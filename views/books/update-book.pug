extends ../layout

block content
    h2= title
    
    include errors
    
    form(action="/books/" + book.id, method="post")
        include form
    form(action="/books/" + book.id + "/delete", method="post", onsubmit="return confirm('Do you really want to delete this book?');", class="form-delete")
        include cancel
        input(type="submit", value="Delete Book")
